# ============================================
# BUILDBOARD PRODUCTION ENVIRONMENT VARIABLES
# ============================================
# Copy this file and fill in your actual values
# ============================================

# Environment
ENVIRONMENT=production

# Database Configuration
DB_HOST=buildboard-db
DB_PORT=5432
DB_USER=buildboard_app
DB_PASSWORD=changeme_in_production
DB_NAME=buildboard_db
DB_SSL_MODE=disable

# Server Configuration
PORT=8080
GIN_MODE=release
FRONTEND_URL=https://yourdomain.com
BACKEND_URL=https://api.yourdomain.com

# Email (SMTP) Configuration
# For Gmail, use app-specific password: https://support.google.com/accounts/answer/185833
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
FROM_EMAIL=your-email@gmail.com
FROM_NAME=BuildBoard